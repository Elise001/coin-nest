# 背景：
1. 市场上充斥着广告+收费模式
2. 记账APP核心是低成本、可持续、异常感知，大部分APP自动记账收费+交互复杂，学习成本高，违背记账初心
   + 低成本指的是学习成本低，交互模式简单，让用户花在APP上的时间减少甚至不用打开
   + 可持续指的是无负担场景下能做到长期坚持，也就是全自动记账能力+正向反馈机制
   + 异常感知是指智能识别异常支出，或短时间内大量支出记录
3. 支付宝、微信、银行卡都有消费记录，记账APP的优势在于提供统一的消费记录入口，和快速发现异常


## v1.0
1. 本地记账闭环：手动记账、模板记账、分类管理、日周月年统计、预算设置。
2. 本地自动记账闭环：通知监听、解析、去重、待确认队列、快捷确认/取消。
3. 本地分析反馈：趋势图、分类占比、预算进度、报告图导出。

## v1.1
1. 自动记账去重升级为三层策略：
   + 屏蔽 App 自身通知回流。
   + 同源去重仅在存在交易流水号时触发。
   + 跨源关联（支付宝/微信与银行卡通知）在时间窗内进行链路合并。
2. 版本号自动化迭代，减少发布版本混淆。

## v1.2
1. 在首页概览新增“本期结论”卡，先给结论再看图表（预算风险/环比变化）。
2. 记一笔页新增“待确认批量处理（全部确认/全部取消）”“快捷金额”“最近分类”，显著减少重复操作
3. 设置页新增“数据安全中心（本地）”，支持导出备份 JSON 到剪贴板、粘贴 JSON 导入（可选覆盖）
4. 打通页面与 ViewModel 回调链路（导入/导出）
5. 主导航文案做了 IA 轻改：首页 / 记一笔 / 我的


## v1.3
1. 异常消费雷达（规则版）已上线：大额单笔、月度异常上涨、分类集中风险、总预算预警、分类预算超额都会生成异常卡
2. 分类预算（二级）已打通：新增 category_budget 表、仓储层读写、ViewModel 状态流、设置页配置与进度展示
3. 备份结构已兼容分类预算（导入导出 JSON 带 category_budgets）
4. 启动闪退修复：DB_VERSION 已提升到 5
### v1.3.1
1. 异常模型升级为“可解释 + 可行动 + 有证据”
2. 异常详情页（弹层）接入


## v1.4（智能分类 MVP）
1. 本地智能分类闭环（Local-First）：
   + 自动记账入库前先执行“规则分类 + 本地学习规则”融合判定。
   + 当规则分类结果为“待分类/自动识别”时，优先尝试本地学习规则自动补全分类。
   + 当本地学习规则命中次数达到阈值后，可覆盖通用规则结果，降低重复手动改类。
2. 本地学习机制：
   + 用户对自动记账流水进行分类纠正后，系统从通知备注中提取关键词并写入本地规则库。
   + 规则维度：交易类型（收入/支出）+ 渠道来源（支付宝/微信/银行卡等）+ 关键词。
   + 规则命中会累积命中次数，用于后续优先级排序（命中次数越高越优先）。
3. 数据可迁移：
   + 本地备份/导入新增智能分类规则数据，换机或重装后可延续已有学习效果。
4. 可解释与可控性增强：
   + 自动分类命中时会在备注追加 `[SMART:关键词#命中次数]` 标记，便于问题排查和用户理解分类来源。
   + 设置页新增“智能分类学习状态”卡片，可查看规则总数、高置信规则数与高频关键词。
   + 支持一键清空本地学习规则（不影响历史流水），用于重置学习偏差。


## v1.5（正向反馈与留存）
1. 连续记录体系（本地计算）：
   + 新增“当前连续记录天数”“历史最长连续天数”“本月活跃记录天数”三项留存指标。
   + 指标全部基于本地已确认流水计算，不依赖联网服务。
2. 轻量徽章机制（低打扰）：
   + 新增连续记录徽章阈值：3天、7天、14天、30天。
   + 达到阈值后在首页“成就与留存”卡片展示已解锁徽章。
3. 达成庆祝反馈（即时但克制）：
   + 当日连续记录达到里程碑阈值时，展示一句轻量庆祝文案，增强正反馈。
   + 不弹窗、不打断主流程，避免噪音和打扰。

## v2.0（视觉体系升级与结构重构）
1. 全局信息架构重构（IA）：
   + 主导航升级为四栏：`首页（结论）/记一笔（动作）/洞察（分析）/我的（设置）`。
   + 首页首屏收敛为三块核心信息：本期结余、预算风险、异常提醒，贯彻“先答案后图表”。
   + 洞察页从首页独立，承担分析场景，降低首页信息负担。
2. 洞察页结构标准化：
   + 按“异常 -> 趋势 -> 结构 -> 明细”顺序组织信息，减少用户来回切换理解成本。
   + 周/月/年三个观察维度统一使用同一分析骨架，便于后续扩展更多洞察规则。
3. 视觉与动效升级（克制增强）：
   + 主色体系切换为深青绿主色（#0D7377）+ 中性背景，降低高饱和大面积色块造成的视觉压力。
   + 图表动效时长收敛到 260ms，减少“炫技感”，强调状态反馈与流畅过渡。
4. 字体与可读性优化（MVP）：
   + 金额与时间区域启用等宽数字字体（Monospace），提升数字扫描效率和秩序感。
   + 正文继续使用系统中文字体，避免中英混排割裂和兼容性风险。
5. 设置页体验增强：
   + 自动记账模块新增“健康体检进度条”，将权限配置状态从文本提示升级为可视化进度。
   + 保留原有分组结构，降低学习成本并保持老用户迁移顺滑。


## v3.0（UI重构）
1. 主 Tab 从顶部改到底部，贴合移动端拇指操作习惯。
2. 持续做页面拆分，降低单文件复杂度（HomeScreen 大幅瘦身）。
3. 公共 UI 组件抽离（如 MetricPill），减少重复实现
4. 页内状态切换改为 Compose Navigation 真子路由。
5. 去掉“返回卡片”，统一用系统手势返回。
6. 按钮体系统一：主操作、分段控件、快捷 chip 的选中态与反馈统一。
7. 首页、记一笔、洞察、我的 四个tab分别优化
   + 个人信息卡上置，作为总览首屏。
   + 去掉“我的”冗余标题文案。去掉“请选择一个分区进入”提示文案。
   + 个人信息改为整卡可点击，进入子详情页。
   + 入口副文案去掉“支付通知”字样，改为更聚焦表述。
   + 分区入口卡做视觉统一：圆角、边框、强调条、留白节奏优化。
   + 智能学习从重卡改为首页轻摘要卡 + 详情页拆分，降低页面长度与审美疲劳。
   + 数据备份从“粘贴 JSON”改为“导出/导入 JSON 文件”。
   + 备份页增加最近备份时间、大小、状态与最近恢复状态可视化。
   + 备份页增加最近备份时间、大小、状态与最近恢复状态可视化。
   + “模板记账 + 最近分类”重构为“快捷填充”模块。
   + 新增填充模式：完整模板 vs 仅分类，并明确影响范围提示。
   + 快捷金额、快捷填充统一交互样式，减少认知切换。
   + 金额输入强化为主舞台（更高权重、更清晰）。
   + 增加推荐分类快捷入口（基于近期流水与金额区间近似）。
   + 备注默认折叠为“添加备注”，降低首屏长度。
   + 保留“本期结论 + 预算进度”核心框架。
   + 新增“今天流水”卡，展示当天最近 3 条，提升信息密度。
   + 异常提醒保留“查看完整洞察”入口，形成首页到洞察的闭环。
   + 预算卡新增“今日建议可支出”逻辑：按月剩余预算和剩余天数均摊。
   + 对超预算、当月结束、非当前月等场景补充分支提示文案。
   + “本期结论 + 保持记录”信息重叠问题重构为更聚焦的智能总结。
   + 成就与留存从纯文字升级为目标进度表达（更有激励感）。
   + 洞察改为“总览 + 子页面”结构，减少超长滚动。
   + 周视图：移除长列表，改为“周流水详情页”（周柱状图 + 饼图 + 周内任意天明细）。
   + 月视图：移除“上一天/下一天”方式，改为“月日历选日 + 可视化 + 当天明细”。
   + 年视图：去掉流水列表，改为“年度分析页”（年内趋势 + 分类占比 + 历史年份切换）。
   + 异常详情独立为入口页，避免总览页面拥挤。
     
     
     
     
     
     
     
     
## v3.1（自动记账权限页精简 + 调试可视化 + 洞察性能优化）
1. 自动记账与权限页精简（更短、更直观）：
   + 页面信息密度重构：去掉冗长权限说明，保留“必要权限完成度 + 自动记账状态 + 核心操作按钮”。
   + 操作分组更清晰：必要权限（通知监听/微信支付通知/支付宝支付通知）与优化项（后台保活/自启动）分区展示。
   + 非必要权限说明收敛到按钮文案，减少阅读负担，首屏可读性提升。
2. 厂商机型适配优化（小米/红米）：
   + 检测到小米/红米系设备时，增加“自启动（小米/红米建议）”快捷入口。
   + 自启动设置支持直达系统页面并带失败兜底，降低用户排查路径长度。
3. 调试信息可视化增强：
   + 自动记账页新增“最近连接”“最近监听”调试信息，均带时间（精确到秒）。
   + 最近监听展示来源（如微信/支付宝）与最近一条通知预览，默认仅展示 1 条，便于快速判断链路状态。
4. 调试提示中文化：
   + debugPopup 弹窗文案从英文技术串映射为中文短句（如“通知监听已连接”“自动记账未入库”等）。
   + 常见失败原因（去重/关联/解析失败/监听异常）统一中文表达，现场调试更易理解。
5. 洞察页内存与性能优化（长期使用更稳）：
   + 周期统计从“全量流水内存计算”改为“SQL 聚合计算”（月按天趋势、年按月趋势、分类占比）。
   + 月/年明细改为分页加载，新增“加载更多”交互，避免一次性拉取大列表导致内存攀升。
   + 在数据量持续增长场景下，保持洞察渲染速度与内存占用更可控。